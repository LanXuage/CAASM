[project]
name = "worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "shared",
    "aiokafka==0.11.0",
    "msgpack>=1.1",
    "pydantic>=2.9",
]

[tool.uv.sources]
shared = { workspace = true }