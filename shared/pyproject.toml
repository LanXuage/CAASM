[project]
name = "shared"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "nebula3-python==3.8.2",
    "pydantic>=2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]